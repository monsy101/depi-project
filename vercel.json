{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "functions": {
    "main.py": {
      "excludeFiles": "{deployment/**,lora/**,models/**,scripts/**,**/*.{pt,bin,safetensors,ckpt,pth,zip}}"
    }
  }
}
